/**
 * receptionos-ui — public entrypoint
 *
 * This is the only file consuming apps should import from
 * (`import { EmptyState } from "receptionos-ui"`).
 *
 * Subpath imports are also supported:
 *   import { brandColors } from "receptionos-ui/tokens"
 */
export * from "./primitives";
export * from "./patterns";
export * as tokens from "./tokens";
